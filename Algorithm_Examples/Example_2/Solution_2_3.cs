/* using System;

namespace MyApp // Note: actual namespace depends on the project name.
{
    // Creating the algorithm that gives the sum of the squares of the entered numbers.
    internal class Solution_2_3
    {
        static void Main(string[] args)
        {
            try
            {
                Console.WriteLine("Enter the 2 numbers you want to calculate the sum of their squares.");
                double sum = Math.Pow(int.Parse(Console.ReadLine()), 2) + Math.Pow(int.Parse(Console.ReadLine()), 2);
                Console.WriteLine(sum);
            }
            catch
            {
                Console.WriteLine("Please enter a value of number type only.");
            }
        }
    }
}
*/